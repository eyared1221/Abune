import { SectionPlaceholder } from "@/components/ui/section-placeholder";

export function AppointmentForm() {
  return (
    <SectionPlaceholder
      title="Appointment form"
      description="Reusable appointment form components will live here."
    />
  );
}
