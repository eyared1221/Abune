import { SectionPlaceholder } from "@/components/ui/section-placeholder";

export function AppointmentTable() {
  return (
    <SectionPlaceholder
      title="Appointment table"
      description="Tabular appointment listings will live here."
    />
  );
}
